interface DashboardProps {
    consumedEnergy: string;
    compensedEnergy: string;
    totalCostWithouGD: string;
    economyGD: string;
}

export const dashboard = async (): Promise<DashboardProps> => {
    try {
        
        return ;
    } catch (error) {
        
    }
}